// import React from "react";

// import {
//     isBrowser,
//     isMobile,
//     mobileVendor,
//     mobileModel,
//     osName,
//     osVersion,
//     browserName
// } from "react-device-detect";

function get_device_info() {
    return "xxx";

    // let device_info = "";
    // if (isMobile) {
    //     device_info = `${mobileVendor}:${mobileModel} ${osName}:${osVersion}`;
    // } else if (isBrowser) {
    //     device_info = `${browserName} ${osName}:${osVersion}`;
    // }

    // return device_info;
}

export default get_device_info;